import { DetectedFacesSnapPathPipe } from './detected-faces-snap-path.pipe';

describe('DetectedFacesSnapPathPipe', () => {
  it('create an instance', () => {
    const pipe = new DetectedFacesSnapPathPipe();
    expect(pipe).toBeTruthy();
  });
});
