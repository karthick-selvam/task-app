import { ParseDateTimePipe } from './parse-date-time.pipe';

describe('ParseDateTimePipe', () => {
  it('create an instance', () => {
    const pipe = new ParseDateTimePipe();
    expect(pipe).toBeTruthy();
  });
});
