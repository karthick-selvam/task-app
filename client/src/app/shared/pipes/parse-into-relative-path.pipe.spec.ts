import { ParseIntoRelativePathPipe } from './parse-into-relative-path.pipe';

describe('ParseIntoRelativePathPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseIntoRelativePathPipe();
    expect(pipe).toBeTruthy();
  });
});
